<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:ignore="SelectedPhotoAccess">

    <!-- ✅ Features -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <!-- ✅ Core permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- ✅ Storage permissions for different Android versions -->
    <!-- Android 13 (API 33) and above -->
    <uses-permission
        android:name="android.permission.READ_MEDIA_IMAGES"
        android:required="false" />

    <!-- Android 12 and below -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WeGoo"
        tools:replace="android:label,android:icon,android:theme"
        tools:targetApi="33">

        <!-- ✅ Entry point -->
        <activity
            android:name=".OnboardingActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ✅ Auth-related activities -->
        <activity android:name=".LoginActivity" android:exported="false" />
        <activity android:name=".UserSignUpActivity" android:exported="false" />
        <activity android:name=".ProviderLoginActivity" android:exported="false" />

        <!-- ✅ User section -->
        <activity android:name=".HomeActivity" android:exported="false" />
        <activity android:name=".UserHomepageActivity" android:exported="false" />
        <activity android:name=".BookingActivity" android:exported="false" />
        <activity android:name=".UserProfileActivity" android:exported="false" />
        <activity android:name=".AboutUsActivity" android:exported="false" />
        <activity android:name=".ContactProviderActivity" android:exported="false" />
        <activity android:name=".CompareTableActivity" android:exported="false" />
        <activity android:name=".UserHistoryActivity" android:exported="false" />

        <!-- ✅ Provider section -->
        <activity android:name=".ProviderHomepageActivity" android:exported="false" />
        <activity android:name=".CustomerListActivity" android:exported="false" />
        <activity android:name=".ProviderProfileSettingsActivity" android:exported="false" />

        <!-- ✅ Vehicle-related -->
        <activity
            android:name=".UpdateVehicleActivity"
            android:exported="false"
            android:label="Update Vehicle" />

        <activity
            android:name=".HistoryActivity"
            android:exported="false"
            android:label="History" />

        <!-- ✅ OpenCV related -->
        <activity
            android:name=".OpenCVDetectionActivity"
            android:exported="true"
            android:label="OpenCV Detection" />

        <activity
            android:name=".CameraTestActivity"
            android:exported="true"
            android:label="OpenCV Camera Test"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

    </application>
</manifest>
